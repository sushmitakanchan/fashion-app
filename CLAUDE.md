<!--
  Claude Code reads this file. The project's agent guide lives in AGENTS.md so it
  is shared with every agent (Cursor, Codex, etc.) from one source of truth.
  The import below pulls it in — add project rules to AGENTS.md, not here.
-->
@AGENTS.md

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `sushmitakanchan/fashion-app`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five default canonical triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Uses a single-context layout. See `docs/agents/domain.md`.
